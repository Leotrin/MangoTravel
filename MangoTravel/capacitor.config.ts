import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mangotravel.mk',
  appName: 'MangoTravel',
  webDir: 'www',
  server: {
    androidScheme: 'https',
    // url: '192.168.1.121',
  },
};

export default config;
